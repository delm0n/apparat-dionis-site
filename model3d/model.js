import React from "react";
import ReactDOM from "react-dom/client";
import ModelContainer from "./ModelContainer";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<ModelContainer />);
